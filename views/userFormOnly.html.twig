<div class="well">
                {{ form_start(userForm,{'attr':{'class':'form-horizontal'}}) }}
                <div class="form-group">
                    {{ form_label(userForm.username,null,{'label_attr':{'class':'col-md-4 control-label'}}) }}
                    <div class="col-md-6">
                        {{ form_widget(userForm.username,{'attr':{'class':'form-control','placeholder':'Username'}}) }}
                        {{ form_errors(userForm.username) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(userForm.password.first,null,{'label_attr':{'class':'col-md-4 control-label'}}) }}
                    <div class="col-md-6">
                        {{ form_widget(userForm.password.first,{'attr':{'class':'form-control','placeholder':'Password'}}) }}
                        {{ form_errors(userForm.password.first) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(userForm.password.second,null,{'label_attr':{'class':'col-md-4 control-label'}}) }}
                    <div class="col-md-6">
                        {{ form_widget(userForm.password.second,{'attr':{'class':'form-control','placeholder':'Password'}}) }}
                        {{ form_errors(userForm.password.second) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(userForm.role,null,{'label_attr':{'class':'col-md-4 control-label'}}) }}
                    <div class="col-md-6">
                        {{ form_widget(userForm.role,{'attr':{'class':'form-control'}}) }}
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-offset-4 col-md-3">
                        <input class="btn btn-primary" type="submit" value="save">
                    </div>
                </div>
                {{ form_rest(userForm) }}
                {{ form_end(userForm) }}
            </div>
